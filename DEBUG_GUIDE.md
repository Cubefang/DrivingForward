# GS场景补全 - 调试输出说明

## 概述

为了方便验证中间结果，所有主要功能模块都添加了可控的调试输出。通过配置文件中的 `gs_completion_debug` 参数可以开启/关闭调试信息。

**输出策略**：
- 每个epoch仅输出第一个batch的信息
- 只输出相机0的第一个样本（batch中的第0个）
- 只显示关键指标，避免信息过载
- 使用中文说明，便于快速理解

## 配置方法

在配置文件中设置：

```yaml
training:
  gs_completion_debug: True   # 开启调试输出
  # gs_completion_debug: False  # 关闭调试输出（默认）
```

## 重置计数器（可选）

默认情况下，整个训练过程只会在第一个batch输出一次debug信息。

如果你想要每个epoch都输出一次，需要在训练循环中每个epoch开始时调用：

```python
# 在trainer中，每个epoch开始时
model.reset_debug_counter()
```

如果不调用此方法，debug信息只会在训练开始时输出一次，这对于验证代码是否正常工作已经足够。

## 调试输出内容

### 1. 教师模型-3D投影 (teacher_warping.py)

**输出示例**:
```
[教师模型-3D投影] 样本0:
  解遮挡区域占比: 12.34% (12345像素)
  补全深度范围: 5.12~80.46m
```

**关键指标**:
- 解遮挡区域占比：应在5%-20%之间，表示有合理的遮挡区域
- 补全深度范围：应符合场景深度分布

### 2. 深度膨胀 (depth_utils.py)

**输出示例**:
```
[深度膨胀] 样本0:
  零值占比: 34.56% → 23.45%
  深度范围: 1.50~80.00m
```

**关键指标**:
- 零值占比：膨胀后应减少，说明遮挡区域被填充
- 深度范围：应保持合理的场景深度

### 3. 3D坐标计算 (depth_utils.py)

**输出示例**:
```
[3D坐标计算] 样本0:
  深度偏移范围: -0.987~0.876
  实际深度范围: 1.23~85.68m
  XYZ范围: X=[-45.12,45.23] Z=[1.23,85.68]
```

**关键指标**:
- 深度偏移：应在[-1, 1]之间，网络预测的偏移量
- 实际深度：膨胀深度+偏移后的最终深度
- XYZ范围：3D坐标应符合场景尺度

### 4. 补全网络预测 (gs_completion_network.py)

**输出示例**:
```
[补全网络预测] 样本0:
  深度偏移: -0.987~0.876
  尺度范围: 0.0001~0.0099
  不透明度: 0.234±0.123
  有效性均值: 0.234
```

**关键指标**:
- 深度偏移：网络预测的深度调整
- 尺度范围：GS球的大小，应该较小（<0.01）
- 不透明度：平均值和标准差，反映预测的置信度
- 有效性均值：表示多少区域被认为需要补全（10%-40%合理）

### 5. 场景补全执行 (drivingforward_model.py)

**输出示例**:
```
[场景补全执行] 相机0:
  补全网络已成功执行
```

**说明**：确认补全网络在指定相机上成功执行

### 6. 教师真值生成 (drivingforward_model.py)

**输出示例**:
```
[教师真值生成] 相机0:
  教师真值已成功生成
```

**说明**：确认教师真值通过3D投影成功生成

### 7. 补全损失计算 (drivingforward_model.py)

**输出示例（成功）**:
```
[补全损失计算] 相机0:
  RGB损失: 0.0123
  深度损失: 0.0012
  总损失: 0.0135
  有效像素数: 12345
```

**输出示例（缺失数据）**:
```
[补全损失计算] 相机0:
  ⚠️ 缺少必要的输出数据
  缺少: 补全渲染结果
```

**关键指标**:
- RGB损失：应随训练逐渐下降
- 深度损失：辅助监督信号
- 有效像素数：参与损失计算的像素数量

## 使用建议

### 首次运行检查清单

开启调试后，重点关注：

1. ✅ 解遮挡区域占比在5%-20%之间
2. ✅ 深度膨胀后零值占比减少
3. ✅ 有效性均值在10%-40%之间
4. ✅ 补全损失随训练下降
5. ✅ 没有"缺少必要的输出数据"警告

### 正式训练

验证无误后，关闭调试输出：

```yaml
training:
  gs_completion_debug: False
```

## 代码标记

所有修改的代码前都添加了 `#claude` 注释，方便快速定位：

```bash
grep -r "#claude" models/
```

## 故障排查

### 问题1: 解遮挡区域占比为0

**可能原因**：
- Z-Test阈值设置不合理
- 相机运动太小

**解决方法**：
- 调整 `teacher_depth_threshold` 参数（默认0.1）
- 检查数据集是否包含足够的相机运动

### 问题2: 有效性均值过高/过低

**可能原因**：
- 网络未收敛
- 训练数据不足

**解决方法**：
- 继续训练
- 检查validity_threshold参数（默认0.5）

### 问题3: 缺少必要的输出数据

**可能原因**：
- 渲染模块未实现
- 数据流中断

**解决方法**：
- 检查 `pred_gaussian_imgs()` 是否生成了补全渲染结果
- 确认数据加载器提供了t+1时刻的数据

## 性能影响

调试输出对训练速度影响极小（<1%），可以放心使用。
